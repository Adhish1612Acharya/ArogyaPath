
import { useNavigate } from "react-router-dom";

const usePost = () => {
  const navigate = useNavigate();


  const createPost = async (url:string,postData: any) => {
  return {
  
  };
};
}

export default usePost;
